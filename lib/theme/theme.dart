import 'package:flutter/material.dart';

import 'app_theme.dart';

/// Re-export unified theme (legacy imports).
ThemeData get lightMode => AppTheme.light();
ThemeData get darkMode => AppTheme.dark();
